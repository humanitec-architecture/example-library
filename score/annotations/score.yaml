apiVersion: score.dev/v1b1
metadata:
  name: my-workload
  annotations:
    podscoreannotationkey: podscoreannotationvalue

containers:
  demo:
    image: registry/my-image:1.0.0
service:
  ports:
    http:
      port: 80
      targetPort: 8080